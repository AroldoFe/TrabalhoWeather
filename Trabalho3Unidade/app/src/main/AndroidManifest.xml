<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalho3unidade">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.views.WeatherActivity"></activity>
        <activity android:name=".ui.login.views.CityActivity" />

        <receiver
            android:name=".ui.login.broadcast.MunicipiosReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ui.login.views.StateActivity" />
        <activity android:name=".ui.login.views.MainActivity" />
        <activity android:name=".ui.login.views.RegisterActivity" />
        <activity
            android:name=".ui.login.views.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>